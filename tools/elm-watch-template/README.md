# elm-watch 开发模板

这是一个使用 `elm-watch` 进行 `elm` 开发的最小模板。

```bash
# 安装依赖项
yarn

# 启动开发
yarn  start

## 如果应用不是 Brower.application，则可接入在浏览器中打开 index.html
## 如果应用是 Brower.application，则需要启动一个 webserver，可选择使用 microserver

# 构建
yarn build

# 最小化js
yarn mini
```
